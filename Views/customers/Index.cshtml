@model IEnumerable<webApp.Models.customerDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "AddOrEdit")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.userName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.designation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.customerAccess)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.adminId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.adminUserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.userName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.designation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.customerAccess)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.adminId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.adminUserName)
        </td>
        <td>

            <a href="@Url.Action("AddOrEdit","customers", new { id = item.Id })" class="btn btn-default">Edit</a>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
        @Html.ActionLink("Details", "Details", new { id=item.Id }) |*@
            <a href="@Url.Action("Delete","customers", new { id = item.Id })" class="btn btn-default">Delete</a>
            @*@Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
        </td>
    </tr>
}

</table>
